// Copy this file to environment.development.ts (git-ignored) for `ng serve` / `ng build -c development`.
// Put your personal Cloud Sync (Google Apps Script) URL below, or leave it empty to use the local API / browser storage.
export const environment = {
  production: false,
  googleSheetsUrl: '',
};
